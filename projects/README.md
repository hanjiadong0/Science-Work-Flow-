# Local Project Links

This folder is for local links into projects you want to use from inside `Science-Work-Flow-`.

Current local setup:

- `LeanCourse25` is a local junction to `C:\Users\Anwender\LEANCOURSE\LeanCourse25`

The junction is intentionally ignored by git so the large external Lean project does not get committed into this repository.
